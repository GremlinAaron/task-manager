# 四象限任务管理器

一个可安装到手机主屏幕的四象限任务管理器 PWA，支持项目、任务、子项、本地保存和离线访问。

## 本地使用

直接用浏览器打开 `index.html` 即可使用。若要启用 PWA 离线缓存和手机安装能力，请通过 `localhost` 或 HTTPS 访问。

## GitHub Pages

仓库推送到 GitHub 后，下面的 GitHub Actions 会自动发布到 GitHub Pages。
